#include<bits/stdc++.h>                          /// cpp + ctrl+j
#define ll long long
#define dubb long double
#define pi acos(-1)
using namespace std;
const ll N=1000;
char s[N], t[N];

int main()
{
    cin >> s >> t;
    int length_s = strlen(s);
    int length_t = strlen(t);
    if (strcmp(s,t)<0)
    {
        cout<<s;
    }
    else cout<<t;






    return 0;
}
